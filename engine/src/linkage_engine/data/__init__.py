"""Data access tier: everything that touches the outside world.

Downloads, gzip streams, the wordfreq dataset, the WordNet corpus, and
pickles. Implements the interfaces declared in `..domain.ports`.
"""
