# Development puzzle

`2026-10-01.json` is a **hand-encoded development fixture**, not output of
`linkage export`. It exists so Phase 3 and Phase 4 have a real puzzle to
render and play against while the approve-to-schedule question is still
open (planning.md 7.7.3).

Its chain is a genuine round-1 approval:
`WHALE -> ocean -> blue -> sky -> birds -> WINGS`.

`linkage export` overwrites this directory. Nothing here is authoritative.
